var keys = {
    PIPELINE: 'pipeline',
    STAGES: 'stages',
    STAGE: 'stage'
}

var keysArray = [keys.PIPELINE, keys.STAGES, keys.STAGE];
console.log(keys.PIPELINE);
module.exports = {
    keys,
    keysArray
}