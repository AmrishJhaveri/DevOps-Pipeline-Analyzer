// The module which contains the application of logic of fetching the data from github api and cloning the repos.
const app=require('./app/q2/q2.js');

//Call the getRepoData() function in the module.
 app.startProcess();